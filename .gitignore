# ==============================================================
#  .gitignore — Visual Studio 2026 / Visual C++ (vcxproj)
#  Windows 전용 MSVC(MSBuild) 프로젝트 기준
# ==============================================================

# --------------------------------------------------------------
#  Visual Studio 로컬 폴더
#  솔루션 사용자 옵션, IntelliSense DB, 서버 상태 등 전부 로컬 전용
# --------------------------------------------------------------
.vs/

# --------------------------------------------------------------
#  빌드 출력 / 중간 산출물 폴더
#  vcxproj 기본 출력: $(Platform)\$(Configuration)\  (예: x64\Debug\)
# --------------------------------------------------------------
[Dd]ebug/
[Dd]ebugPublic/
[Rr]elease/
[Rr]eleases/
x64/
x86/
[Ww][Ii][Nn]32/
[Aa][Rr][Mm]/
[Aa][Rr][Mm]64/
[Bb]in/
[Bb]uild/
[Oo]bj/
[Oo]ut/
[Ll]og/
[Ll]ogs/

# --------------------------------------------------------------
#  사용자별 설정 파일 (개인 디버깅 설정 등, 커밋 대상 아님)
# --------------------------------------------------------------
*.vcxproj.user
*.suo
*.user
*.userosscache
*.sln.docstates

# --------------------------------------------------------------
#  컴파일러 / 링커 중간 산출물
# --------------------------------------------------------------
*.obj
*.o
*.pch
*.pdb
*.idb
*.ilk
*.exp
*.tlh
*.tli
*.res

# --------------------------------------------------------------
#  MSBuild 로그 / 캐시
# --------------------------------------------------------------
*.tlog
*.lastbuildstate
*.log
*.VC.db
*.VC.VC.opendb

# --------------------------------------------------------------
#  리소스 컴파일러 / 구버전 VS 잔여물 (있어도 무해)
# --------------------------------------------------------------
*.aps
*.ncb
*.sdf
*.opensdf
*.cachefile
ipch/

# --------------------------------------------------------------
#  패키지 매니저
# --------------------------------------------------------------
# vcpkg (manifest 모드에서 설치되는 실제 라이브러리)
vcpkg_installed/
# NuGet (네이티브 패키지)
packages/
*.nupkg

# --------------------------------------------------------------
#  프로파일링 / 분석 / 테스트 결과
# --------------------------------------------------------------
*.psess
*.vsp
*.vspx
*.sap
[Tt]est[Rr]esult*/

# --------------------------------------------------------------
#  OS / 에디터 잡파일
# --------------------------------------------------------------
Thumbs.db
Desktop.ini
.DS_Store

# ==============================================================
#  [선택] CMake를 나중에 도입한다면 주석 해제
# --------------------------------------------------------------
# out/
# build/
# CMakeCache.txt
# CMakeFiles/
# CMakeUserPresets.json

# ==============================================================
#  [참고] *.lib / *.dll / *.exe 는 확장자로 막지 않았습니다.
#  위의 출력 폴더 규칙으로 빌드 산출물은 대부분 걸러지고,
#  서드파티 prebuilt 바이너리(예: libs/foo.lib)를 커밋하는
#  경우를 막지 않기 위함입니다.
#  빌드 산출물을 확장자로 확실히 막고 싶으면 아래를 해제하고,
#  커밋해야 할 prebuilt 는 !libs/**/*.lib 식으로 예외 처리하세요.
# --------------------------------------------------------------
# *.exe
# *.dll
# *.lib
